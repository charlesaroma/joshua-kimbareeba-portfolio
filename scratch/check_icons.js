import * as Lucide from 'lucide-react';
console.log(Object.keys(Lucide).filter(k => k.toLowerCase().includes('git') || k.toLowerCase().includes('link') || k.toLowerCase().includes('youtube') || k.toLowerCase().includes('face')));
